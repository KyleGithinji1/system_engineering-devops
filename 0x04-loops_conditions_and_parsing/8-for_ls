#!/usr/bin/env bash
# Displays files in current directory after the first dash (-) using a for loop

for file in *; do
    # Skip hidden files
    if [[ -f "$file" && "$file" != .* ]]; then
        echo "${file#*-}"
    fi
done
